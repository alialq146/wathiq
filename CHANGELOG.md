# سجل التغييرات · Changelog

كل الإصدارات البارزة لمنصة **وثّق**. الأحدث في الأعلى.
الصيغة مستوحاة من [Keep a Changelog](https://keepachangelog.com/).

---

## [0.8.0] — 2026-07-02 · تصدير المتطلبات
### أُضيف
- زر **«تصدير»** في شاشة المتطلبات يفتح نافذة اختيار الصيغة.
- **PDF:** وثيقة منسّقة كاملة (غلاف + كل متطلب مع معاييره وقواعده وأسئلته) تُطبع أو تُحفظ عبر نافذة الطباعة — دعم عربي/RTL مثالي لأن المتصفح هو من يرسم النص.
- **Word (‏.doc):** المستند نفسه بصيغة يفتحها Word للتعديل.
- **Excel (‏.csv):** جدول المتطلبات بترميز UTF-8 (مع BOM) ليعرض العربية بشكل صحيح.
### تقني
- التصدير **بدون أي مكتبات خارجية** (اعتمادًا على طباعة المتصفح + Blob) لضمان سلامة العربية وخفّة الحزمة.

## [0.7.0] — 2026-07-01 · دورة المتطلب والسياق
### أُضيف
- **ربط العناصر بالمتطلب:** صارت معايير القبول وقواعد العمل والأسئلة المفتوحة مرتبطة بمتطلب محدّد (`requirementId`)، وتُعرَض مفلترة في التفاصيل.
- **إضافة معيار قبول** فعليًا من التفاصيل (محرّر سطري، ⌘/Ctrl+Enter للحفظ)، و**تبديل حالة الإنجاز** بالنقر على المربّع.
- **الإجابة عن الأسئلة المفتوحة** وحفظها وتعديلها لاحقًا، مع عرض الإجابة داخل البطاقة.
- **رؤى الذكاء الاصطناعي** في لوحة التفاصيل صارت مبنية على بيانات المتطلب الفعلية (نسبة الإنجاز، الأسئلة بلا إجابة، التوصيات).
- **صفحات السياق:** «أصحاب المصلحة» و«قواعد العمل» و«سجل التدقيق» صارت شاشات حقيقية بدل «قيد الإعداد».
- **سجل تدقيق (AuditEvent):** يُسجَّل كل تغيير (حالة، إنشاء/تعديل/حذف، إضافة معيار، إجابة، استيراد) ويظهر كخط زمني.
- **مبدّل المشروع** و**أيقونة الإعدادات** صارا يفتحان قوائم منبثقة فعّالة (تعرض مصدر البيانات والإصدار وعدد المتطلبات).
### ملاحظة تقنية
- أعمدة قاعدة البيانات الجديدة كلها اختيارية (nullable)، فيطبّقها `prisma db push` وقت النشر دون فقد بيانات.

## [0.6.0] — 2026-07-01 · سير الاعتماد والحالة
### أُضيف
- زرّا **«طلب الاعتماد»** و**«طلب معلومات إضافية»** في لوحة التفاصيل يغيّران حالة المتطلب فعليًا في القاعدة عبر Server Action، مع حفظ فوري وتحديث شارة الحالة في التفاصيل والقائمة.
- حالة تحميل (أيقونة دوّارة)، ورسائل نجاح/خطأ واضحة، وتعطيل ذكي للأزرار عند بلوغ الحالة نفسها أو عند عدم الاتصال بقاعدة البيانات.
- البحث والتصفية والترتيب في قائمة المتطلبات (بحث بالرقم/النص · فلترة بالحالة والأولوية · ترتيب) مع زر **«مسح كل الفلاتر»**.

## [0.5.0] — 2026-06-30 · الذكاء الاصطناعي
### أُضيف
- تحليل وثائق المتطلبات فعليًا عبر **Claude (claude-opus-4-8)** مع مخرجات منظّمة.
- نقطة `/api/analyze` (محميّة بمفتاح `ANTHROPIC_API_KEY`).
- زر **«حفظ في القاعدة»** يضيف المتطلبات المستخرجة إلى Neon.
- شاشة تحليل تفاعلية: لصق نص → تقدّم مباشر → نتائج حقيقية (ملخّص، خطوات، توصيات، ثقة).

## [0.4.0] — 2026-06-30 · لوحة التعديل
### أُضيف
- إضافة/تعديل/حذف المتطلبات من الواجهة عبر Server Actions تكتب في القاعدة.
- نافذة نموذج كاملة + أزرار تعديل/حذف على كل بطاقة.
### تغيّر
- سكربت البذرة صار **«مرة واحدة»** حتى لا تُمسح التعديلات عند إعادة النشر.

## [0.3.0] — 2026-06-30 · قاعدة البيانات
### أُضيف
- طبقة بيانات بـ **Prisma + Neon Postgres** مع **تراجع آمن** للبيانات الافتراضية.
- نقطة `/api/health` تكشف مصدر البيانات (`database` / `fallback`).
- نطاق مخصص **`wathiq-ai.vercel.app`**.

## [0.2.0] — 2026-06-30 · النشر والتأمين
### تغيّر
- ترقية Next.js إلى `15.5.19` (إصدار مُصحَّح، **٠ ثغرات**).
- إعادة كتابة هوية الـ commits باسم المطوّر لحل حظر نشر Vercel.
### أُضيف
- النشر على Vercel (Root Directory = `wathiq-app`).
- `README.md` ثنائي اللغة + نقل ملاحظات التسليم إلى `HANDOFF.md`.

## [0.1.0] — 2026-06-30 · التطبيق الأساسي
### أُضيف
- تحويل حزمة التصميم إلى تطبيق **Next.js (App Router) + TypeScript**.
- نقل توكنز التصميم و١٣ مكوّنًا إلى React/CSS (عربي أولاً · RTL).
- الشاشات الأربع: نظرة عامة، المتطلبات، تفاصيل المتطلب، تحليل وثّق.
- رفع المشروع على GitHub (`alialq146/wathiq`).
