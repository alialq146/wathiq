**PriorityLabel** — priority indicator with a signal-bar glyph (more bars = higher).

```jsx
<PriorityLabel level="critical" />
<PriorityLabel level="medium" showLabel={false} />
```

Levels: `critical` `high` `medium` `low`. Set `showLabel={false}` for icon-only in dense tables.
