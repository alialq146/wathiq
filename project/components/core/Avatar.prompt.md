**Avatar** — circular user avatar for stakeholders, assignees, and comments. Initials + deterministic tint when no image.

```jsx
<Avatar name="سارة العتيبي" size={32} />
<Avatar name="Omar Faisal" src="/omar.jpg" size={28} />
```

`size` in px (default 32). Pair several in a `StakeholderGroup`.
