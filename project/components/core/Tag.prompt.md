**Tag** — outlined keyword chip for modules, domains, and removable filter pills.

```jsx
<Tag color="blue">المدفوعات</Tag>
<Tag color="teal" onRemove={() => {}}>أولوية: عالية</Tag>
```

Colors: `slate` `blue` `teal`. Pass `onRemove` to show a × dismiss control.
