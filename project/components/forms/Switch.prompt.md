**Switch** — toggle for binary settings such as auto-analysis or notifications.

```jsx
<Switch label="تحليل تلقائي عند الرفع" defaultChecked />
<Switch checked={on} onChange={setOn} />
```

`onChange` receives the next boolean. Controlled or uncontrolled.
